# DB (Neon 등)
DATABASE_URL="postgresql://..."

# 인증 (JWT Access/Refresh 토큰)
JWT_SECRET="your-secret-change-in-production"

# 토스페이먼츠 (카드/토스 선택 시 결제창 연동) — .env는 프로젝트 루트(step4)에 두면 클라이언트·서버 모두 사용
# 프론트: 결제창 호출용 (반드시 VITE_ 접두사). 테스트: test_ck_... 입력
VITE_TOSSPAYMENTS_CLIENT_KEY=
# 백엔드: 결제 승인 API 호출용 (절대 노출 금지). 테스트: test_sk_... 입력
TOSSPAYMENTS_SECRET_KEY=

# 웹 푸시 (주문 접수·상태 변경 시 알림). 미설정 시 푸시 미발송
# 생성: npx web-push generate-vapid-keys (또는 Node: require('web-push').generateVAPIDKeys())
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_MAILTO=mailto:support@example.com
# 프론트: VAPID 공개키 (서버와 동일한 값). Vite 에서만 VITE_ 로 노출
VITE_VAPID_PUBLIC_KEY=
